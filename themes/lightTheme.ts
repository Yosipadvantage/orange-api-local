import { createTheme } from '@nextui-org/react';

export const lightTheme = createTheme({
    type: 'light',
    theme: {
        colors: {
            background: '#FAFAFA',
            default: '#1E91D6',
            primary: 'linear-gradient(220.55deg, #FFD439 0%, #FF7A00 100%);',
            secondary: '#919090'
        },
        fonts: {

        },
    }
});